{
  "name": "patient_indexed_specimen_routing_return1_sensitivity_eval",
  "plan": "experiments/configs/patient_indexed_specimen_routing_benchmark.json",
  "training_manifest": "results/patient_indexed_specimen_routing_recovery2/training/patient_indexed_specimen_routing_pilot_routing/training_manifest.json",
  "algorithms": [
    "gcn_residual_mdl2_network_ddpg_afd",
    "flat_residual_mdl2_network_ddpg_afd"
  ],
  "training_seeds": [0, 1, 2],
  "checkpoint_variants": ["final"],
  "scenarios": ["routing_nominal_return1_sensitivity"],
  "deployment_candidates": [
    {"scale": 0.0, "group_thresholds": [1.0]},
    {
      "scale": 1.0,
      "group_thresholds": [0.0],
      "endpoint_projection": {
        "enabled": true,
        "groups": [
          "specimen_transfer",
          "reagent_transfer",
          "capacity_transfer"
        ],
        "max_endpoints_per_side": 1,
        "min_abs": 0.04
      }
    }
  ],
  "fixed_deployment_candidate": {
    "scale": 1.0,
    "group_thresholds": [0.0],
    "endpoint_projection": {
      "enabled": true,
      "groups": [
        "specimen_transfer",
        "reagent_transfer",
        "capacity_transfer"
      ],
      "max_endpoints_per_side": 1,
      "min_abs": 0.04
    }
  },
  "fixed_checkpoint_variant": "final",
  "validation_replications": 5,
  "validation_seed": 8300000,
  "holdout_replications": 100,
  "holdout_seed": 8400000,
  "max_steps": 52,
  "strict_scenario_clinical_noninferiority": true,
  "clinical_noninferiority": {
    "mode": "paired_ci",
    "z_value": 1.96,
    "margins": {
      "completion_service_level": 0.001,
      "patients_lost": 1.0,
      "patient_ineligibility_during_manufacturing_rate": 0.001
    }
  },
  "output_root": "results/patient_indexed_specimen_routing_recovery2/evaluation/routing_return1_sensitivity"
}
